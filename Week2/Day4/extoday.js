 //exercise 1 

 //function ageCalc(myAge = 23){
 	//let momAge = myAge * 2;
 	//let dadAge = Math.round(momAge * 1.2);
 	//console.log(`My mom's age is ${momAge}`)
 	//console.log(`My dad's age is ${dadAge}`)

// }

// ageCalc(24);

 //exercise 2 

// function ageCalc2(myAge = 23){
 	//let momAge = myAge * 2;
 	//return (`My mom's age is ${momAge}`)
// }

 //console.log(ageCalc(24));


 function info(myAge){
 	if(isNaN (myAge)){
 		
 	}
 	let momAge = myAge * 2 
 	let dadAge = momAge * 1.2
 		age: momAge
 		line: `My mom is ${momAge}, which is twice my age`
 	}
 	return mom

let meAge = 25
let result = Info(meAge)
console.log(result.age)
console.log(result.line)